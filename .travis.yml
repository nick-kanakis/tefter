language: go
sudo: false
go: 
  - 1.8
  - 1.9
  - 1.10
  - tip

script:
  - test -z $(gofmt -s -l $GO_FILES)         # Fail if a .go file hasn't been formatted with gofmt
  - go tool vet .
  - go test -v -race ./...
